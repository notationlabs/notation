---
author: agent
created: "2026-02-26T12:06:35.046Z"
entity: TASK-004
---
Problem: EPIC-002 lists many extraction goals (registry-based orphan deletion, drift detection, eventing, retry policy), but the repo already had a working @notation/reconciler with those behaviors implemented, so the risk was accidentally regressing behavior by refactoring prematurely. I audited packages/reconciler/src/reconciler.ts and the operation generators first, and I found a single correctness issue that could cause real hangs: createStepRunner.run ignored RetryableError policy. Therefore I made only the retry semantics change and added a regression test instead of rewriting the deploy workflow. Grounding: commit a06d997.