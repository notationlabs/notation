---
author: agent
created: "2026-02-26T12:06:03.208Z"
entity: TASK-011
---
Problem: EPIC-005 calls for a generic AST split that is configurable (safe imports predicate, export names, codegen hook), while @notation/esbuild-plugins hardcodes Notation conventions (infra/ prefix and config export). I extracted a new package (esbuild-split-module) rather than refactoring the existing plugin in place because callers depend on current behavior and because wiring the new workspace package into @notation/esbuild-plugins broke vitest module resolution before build outputs existed (errors like Failed to resolve entry for package esbuild-split-module). Grounding: commit f796c16 adds packages/esbuild-split-module with stripStatements, stripExport, extractExport, and plugin wrappers plus tests; @notation/esbuild-plugins behavior is unchanged.