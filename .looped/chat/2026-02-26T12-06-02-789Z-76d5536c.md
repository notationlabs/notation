---
author: agent
created: "2026-02-26T12:06:02.789Z"
entity: TASK-008
---
Problem: EPIC-003 defines StateNode as extensible metadata plus a type string, but packages/state/src/state.ts had groupId and groupType as required first-class fields, which forces consumers to carry Notation-specific grouping even when they do not use it. 

Action: I removed those required fields from the TypeScript type and relied on the existing index signature so backends can store whatever metadata they need without fighting the type system. 

Commit 4a05491 edits packages/state/src/state.ts

State-backend contract tests still pass.
