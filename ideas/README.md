# Ideas

Captured feature ideas distilled from old experiment branches before those branches were deleted.

Each doc states the **minimal idea** relative to `main` at the ref below — not the full stale diff. The original branch is cited only as an archaeology pointer (recover via reflog/GitHub if ever needed).

- Anchored to main: `5fe6221`
